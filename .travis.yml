language: java

jdk: openjdk8
        
services:
  - docker

# skip installation step
install: true

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

script: 
  - cd attsd-spring-project 
  - mvn clean verify -Pjacoco coveralls:report
  - mvn verify -PitTest
  - mvn verify -Pe2e > /dev/null

cache:
   directories:
      - $HOME/.m2
